import { render } from "@testing-library/react"
import { shallow } from "enzyme"
import { BettingLobby } from "./BettingLobby"

test("that jest is working", () => {
  // render(<BettingLobby />)
  // const wrapper = shallow(<BettingLobby />)
  expect(true).toBe(true)
})

export {}
