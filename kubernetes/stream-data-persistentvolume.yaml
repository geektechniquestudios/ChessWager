apiVersion: v1
kind: PersistentVolume
metadata:
  name: stream-data-pv
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  type: gcePersistentDisk
  claimRef:
    name: stream-data
    namespace: default
